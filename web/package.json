{
  "name": "noteapp-web",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "NoteApp Web（Svelte + TS，P0 MVP；数据层可插拔，预留 Tauri 壳）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "serve:dist": "node scripts/serve-dist.mjs",
    "smoke": "node scripts/ui-smoke.mjs",
    "smoke:settings": "node scripts/settings-smoke.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.3",
    "@types/markdown-it": "^14.1.2",
    "svelte": "^5.23.2",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "ws": "^8.21.3"
  }
}
